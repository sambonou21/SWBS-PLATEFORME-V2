APP_NAME="Sam Web Business Services"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=${BASE_URL}

APP_LOCALE=fr
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=fr_FR

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_DIALECT=mysql
DB_HOST=${MYSQL_HOST}
DB_PORT=${MYSQL_PORT}
DB_DATABASE=${MYSQL_DATABASE}
DB_USERNAME=${MYSQL_USER}
DB_PASSWORD=${MYSQL_PASSWORD}

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=reverb
FILESYSTEM_DISK=public
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=${SMTP_HOST}
MAIL_PORT=${SMTP_PORT}
MAIL_USERNAME=${SMTP_USER}
MAIL_PASSWORD=${SMTP_PASS}
MAIL_ENCRYPTION=${SMTP_SECURE}
MAIL_FROM_ADDRESS="no-reply@swbs.site"
MAIL_FROM_NAME="SWBS"

SMTP_HOST=mail.swbs.site
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=no-reply@swbs.site
SMTP_PASS=P@rdondieu12+BSS
MAIL_FROM=SWBS &lt;no-reply@swbs.site&gt;

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher / Reverb (temps réel)
BROADCAST_DRIVER=reverb
REVERB_APP_ID=swbs-app
REVERB_APP_KEY=local-key
REVERB_APP_SECRET=local-secret
REVERB_HOST=127.0.0.1
REVERB_PORT=8080
REVERB_SCHEME=http
REVERB_USE_TLS=false

PUSHER_APP_ID=swbs-app
PUSHER_APP_KEY=local-key
PUSHER_APP_SECRET=local-secret
PUSHER_APP_CLUSTER=mt1

# FEDEPAY (PAIEMENT)
FEDEPAY_PUBLIC=
FEDEPAY_SECRET=
FEDEPAY_MODE=sandbox

# IA
AI_PROVIDER=
AI_API_KEY=
AI_MODEL=gpt-4o-mini
AI_INSTRUCTIONS="Tu es l'assistant conversationnel de la plateforme Sam Web Business Services (SWBS). Tu réponds en français simple et professionnel, tu aides les prospects à comprendre les services et à préciser leurs besoins."

# Admin par défaut (créé automatiquement par le seeder ou npm run init:admin)
ADMIN_EMAIL=admin@swbs.site
ADMIN_PASSWORD=P@rdondieu12+

# Domaine public officiel
BASE_URL=https://swbs.site

VITE_APP_NAME="${APP_NAME}"
VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"
VITE_REVERB_USE_TLS="${REVERB_USE_TLS}"